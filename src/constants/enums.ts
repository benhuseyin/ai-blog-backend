export enum UserStatus {
  active = "active",
  inactive = "inactive",
}

export enum UserRole {
  admin = "admin",
  user = "user",
}

export enum CategoryStatus {
  active = "active",
  inactive = "inactive",
}

export enum BlogStatus {
  active = "active",
  inactive = "inactive",
}
